const fs = require('fs');let s = fs.readFileSync('src/components/settings/SettingsModal.tsx', 'utf-8');s = s.replace('import { useAuth } from \'@/contexts/AuthContext\';', 'import { useAuth } from \'@/contexts/AuthContext\';\nimport { usePlan } from \'@/contexts/PlanContext\';'); s = s.replace('const { profile, saveProfile } = useUserProfile();', 'const { profile, saveProfile } = useUserProfile();\n  const { planTier } = usePlan();'); fs.writeFileSync('src/components/settings/SettingsModal.tsx', s);
